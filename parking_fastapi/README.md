# Parking FastAPI

Реализация API для парковки (заезд/выезд, управление клиентами) на FastAPI + SQLAlchemy.

## Установка и запуск

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload